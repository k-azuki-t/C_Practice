#include <stdio.h>

int main() {
    float base, height;
    float area;

    base = 3.0;
    height = 4.0;

    area = (base * height) / 2.0;

    printf("triangle area %f\n", area);

    return 0;
}